<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        input[type="date"]::-webkit-calendar-picker-indicator {
            filter: invert(1);
            /* Invert the color to white */
        }
    </style>
</head>

<body class="bg-zinc-900">

    <div class="w-full h-full flex justify-center items-center">
        <div class="w-full p-10 text-white max-w-md">
            <h1 class="text-3xl tracking-tighter mt-7 mb-3 text-center">Update User</h1>
            <form action="/update/<%=user._id%>" method="post">
                <div class="flex flex-col">
                    <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent mb-3"
                        name="name" type="text" placeholder="Name" value="<%=user.name%>">
                    <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent mb-3"
                        name="email" type="email" placeholder="Email" value="<%=user.email%>">
                    <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent mb-3"
                        name="phone" type="phone" placeholder="Phone number" value="<%=user.phone%>">
                    <input
                        class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent mb-3 text-gray-400"
                        name="dob" type="date" placeholder="Date of birth" value="<%=user.dob%>">
                    <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent mb-3"
                        name="image" type="text" placeholder="Image URL" value="<%=user.image%>">
                </div>
                <div class="flex justify-start mt-3">
                    <input class="px-5 py-2 bg-yellow-500 rounded-lg ml-3" type="submit" value="Update User">
                </div>
            </form>
        </div>
    </div>
    <div class="absolute top-7 left-10 m-3 text-white">
        <a class="px-5 py-2 bg-blue-500  rounded-lg" href="/read">All users</a>

    </div>

    <div class="absolute top-7 right-10 m-3 text-white">
        <a class="px-5 py-2 bg-blue-500 rounded-lg" href="/">Go Back to Home</a>
    </div>


</body>

</html>